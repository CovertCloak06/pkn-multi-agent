# Core Web Framework
flask>=2.3.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Tools & Utilities
phonenumbers>=8.13.0
requests>=2.31.0
python-whois>=0.8.0
dnspython>=2.4.0
beautifulsoup4>=4.12.0

# RAG & Vector Database (Week 2-3)
chromadb>=0.4.18
sentence-transformers>=2.2.2

# Local Image Generation (100% Private)
torch>=2.0.0
diffusers>=0.25.0
transformers>=4.35.0

# Code Execution Sandbox (Week 4)
docker>=6.1.0

# Database for Agent Evaluation (Week 4)
# sqlite3 is built-in to Python, no need to install

# Optional: Enhanced NLP
# transformers>=4.35.0  # Uncomment if you need full transformer models

# Optional: For better JSON parsing
# pydantic>=2.5.0  # Already might be installed via other dependencies
